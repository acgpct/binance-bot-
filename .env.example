# Binance Spot Testnet — get keys from https://testnet.binance.vision/
# (log in with GitHub, click "Generate HMAC_SHA256 Key")
BINANCE_API_KEY=your_testnet_api_key_here
BINANCE_API_SECRET=your_testnet_api_secret_here

# Set to "true" only when ready to use real funds. Default: testnet.
BINANCE_LIVE=false
